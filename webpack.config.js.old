const path = require('path');
const { CleanWebpackPlugin } = require('clean-webpack-plugin');
const HtmlWebpackPlugin = require('html-webpack-plugin');

module.exports = {
  entry: './src/main.mjs',  // Adjust the entry point based on your project structure
  output: {
    path: path.resolve(__dirname, 'dist'),
    publicPath: '/dist/',
    filename: 'bundle.[contenthash].js',
  },
  module: {
    rules: [
      {
        test: /\.md$/,
        use: 'raw-loader', // or another loader suitable for your needs
      },
    ],
  },
  plugins: [
    new CleanWebpackPlugin(),
    new HtmlWebpackPlugin({
      template: './index.html', // Adjust the template based on your project structure
    }),
  ],
};
